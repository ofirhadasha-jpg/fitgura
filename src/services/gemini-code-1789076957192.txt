Please implement the main feed search and size recommendation logic according to the following strict hierarchy and pipeline:

1. USER MEASUREMENT RESOLUTION (Priority Order)
   - First, check if the user has manually edited/confirmed their measurements (`user_sizes` / edited body measurements).
   - If manual edits exist: Use the manual edits as the single source of truth.
   - If NO manual edits exist: Use the raw measurements generated by the AI body scan.
   - Fallback: If no scan data exists, use the default size preferences declared during registration.
   - Always factor in user parameters: Gender, Fit preference (e.g., Slim, Regular, Loose), and target category.

2. SIZE MATCHING PIPELINE
   - Step 1: Product-Specific Size Chart
     * Check if the product page contains an extracted size chart/table.
     * If a size chart exists: Compare the resolved user measurements (cm/kg) directly against the product's size chart to determine the recommended size (e.g., "L").
   - Step 2: Fallback Standard Conversion (If NO size chart exists)
     * If the product page lacks a size chart, fall back to the standard regional size conversion engine (e.g., applying Asian-sizing offsets, EU/US mappings) using the user's size profile/measurements as the base.

3. INVENTORY & AVAILABILITY CHECK (Strict Rule)
   - After determining the recommended size for a product, check if that recommended size is currently IN STOCK on the product page.
   - CRITICAL REQUIREMENT: NEVER display or recommend a product in the feed if its calculated recommended size is not available for purchase on the product page.

4. FEED RANKING & FALLBACK SAFEGUARDS
   - MANDATORY FILTER: Gender (Products must strictly match the user's gender/unisex preference).
   - RANKING PRIORITIES:
     1. Gender Match
     2. Best Sellers (highest order count / sales volume)
     3. Top Rated (highest customer review scores)
     4. Size Availability
   - NON-BLOCKING GRACEFUL DEGRADATION: Do NOT hard-block the entire feed or return empty results due to overly restrictive measurement filters. If size metrics are borderline, widen the search tolerances so the user always sees top-performing items while prioritizing exact size matches.

Please write/refactor the feed recommendation service and filtering functions to implement this complete flow cleanly with proper TypeScript types.