Please adjust the EU pants size options in `ScanModal.tsx` and `Profile.tsx` based on gender sizing standards:

1. Gender-Aware Men's Pants Sizing Range:
   - For Men (`gender === 'male'`): Set the European pants size range to start at standard Men's EU sizes: **EU 40** or **EU 42** up to **EU 60+** (e.g., EU 40, 42, 44, 46, 48, 50, 52, 54, 56, 58, 60).
   - For Women (`gender === 'female'`): Keep standard Women's EU sizes starting from EU 32 up to EU 52+.
   - Ensure a male user can easily select **EU 42** (or any valid men's size), and that this size is saved properly during the first post-scan edit.

2. Immediate Post-Scan Save & Feed Refresh:
   - Verify that clicking "Save" / "אישור ושמירה" in `ScanModal.tsx` updates `AuthContext` / `useUserProfile` state, `localStorage`, and Supabase `profiles` immediately.
   - Trigger an immediate refresh of the AliExpress feed in `Feed.tsx` matching the saved size (e.g. searching for EU 42 / W30-W31 men's pants when Shoes/Clothing/All tabs are active).